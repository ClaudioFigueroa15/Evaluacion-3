spring:
  application:
    name: search-microservicio
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8088

logging:
  level:
    evaluacion3.search: INFO
