spring:
  application:
    name: pelicula-microservicio
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8081

logging:
  level:
    evaluacion2:
      pelicula: INFO
