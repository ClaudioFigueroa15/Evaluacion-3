spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pelicula_dev
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    defer-datasource-initialization: true
  sql:
    init:
      mode: always

server:
  port: 8081

logging:
  level:
    evaluacion2.pelicula: DEBUG
