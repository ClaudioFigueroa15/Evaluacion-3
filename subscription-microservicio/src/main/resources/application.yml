spring:
  application:
    name: subscription-microservicio
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8085

logging:
  level:
    evaluacion3.subscription: INFO
