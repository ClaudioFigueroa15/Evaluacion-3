spring:
  application:
    name: notification-microservicio
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8089

logging:
  level:
    evaluacion3:
      notification: INFO
