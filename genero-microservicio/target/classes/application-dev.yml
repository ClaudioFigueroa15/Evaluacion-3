spring:
  datasource:
    url: jdbc:mysql://localhost:3306/genero_dev
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    defer-datasource-initialization: true
  sql:
    init:
      mode: always

server:
  port: 8082

logging:
  level:
    evaluacion2.genero: DEBUG
