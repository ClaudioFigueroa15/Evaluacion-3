spring:
  datasource:
    url: jdbc:mysql://localhost:3306/usuario_dev
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    defer-datasource-initialization: true
  sql:
    init:
      mode: always

server:
  port: 8084

logging:
  level:
    evaluacion2.usuario: DEBUG
